# Küçük VPS (çok servis + Docker): varsayılan %75 uyarı sık yanlış pozitif üretir.
# Sunucu gerçekten sıkışırsa crit hâlâ devreye girer.

alarm: ram_usage_warn
on: system.ram
lookup: average -1m percentage of used
every: 30s
warn: $this > 82
crit: $this > 94
info: RAM kullanımı yüksek
to: sysadmin
